#include "main.h"

/**
 * mul - checks for a digit (0 through 9).
 * @a: Variable a
 * @b: Variable b
 * Return: The product of a and b.
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
